# Étape 4 — Recherche avec grep

## Concept

`grep` permet de chercher un mot dans un fichier.

---

## Exemple

```bash
grep error system.log

grep -i error /opt/orion-system/system.log

grep -i warning /opt/orion-system/system.log